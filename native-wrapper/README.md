# Walk Memory v5 native wrapper

Run `npm install`, `npm run build`, `npx cap add android` if `android/` does not exist, then `npx cap sync android` and `npx cap open android`. The native background-geolocation bridge records walks and drives with the screen off. Activity photos are captured with the system camera/file picker and stored with GPS coordinates.
